# MQTT broker (Mosquitto)
MQTT_HOST=192.168.0.10
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=wify-server

# Topic prefix
TOPIC_PREFIX=wify

# HTTP server
HTTP_HOST=0.0.0.0
HTTP_PORT=8000

# How many recent events to keep in memory per device
EVENT_BUFFER_SIZE=200
